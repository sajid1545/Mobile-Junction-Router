import React from 'react';

const Phone = ({phone}) => {

    const { phone_name, image, brand, slug } = phone;
    console.log(phone);

    return (
        <div>
            
        </div>
    );
};

export default Phone;